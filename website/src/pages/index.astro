---
import Base from "../layouts/Base.astro"
---

<Base title="Shannon — nushell + bash">
  <section>
    <div class="flex items-center gap-4 mb-6">
      <img src="/images/shannon-1.webp" alt="Shannon logo" class="w-16 h-16" />
      <div>
        <h1 class="text-lg font-bold text-foreground">Shannon</h1>
        <p class="text-muted">
          A poly-shell built on nushell, with seamless bash compatibility.
        </p>
      </div>
    </div>

    <p class="text-foreground-dark mb-4">
      Nushell is powerful but the world runs on bash. Shannon gives you both —
      press <span class="text-accent">Shift+Tab</span> to switch between
      nushell and bash. Environment variables and working directory sync
      automatically.
    </p>

    <pre class="bg-background-dark p-3 border-l-2 border-border text-foreground-dark text-xs mb-6"><code><span class="text-success">[nu]</span> ~/project &gt; ls | where size &gt; 1mb
...
<span class="text-success">[nu]</span> ~/project &gt; &lt;Shift+Tab&gt;

<span class="text-accent">[brush]</span> ~/project &gt; grep -r "TODO" src/ &amp;&amp; echo "done"
...
<span class="text-accent">[brush]</span> ~/project &gt; &lt;Shift+Tab&gt;

<span class="text-success">[nu]</span> ~/project &gt;</code></pre>

    <div class="mt-6">
      <a href="/docs/getting-started" class="text-primary hover:text-accent">
        &gt; [Get started]
      </a>
    </div>
  </section>
</Base>
