{
  "name": "shannon-website",
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "deploy": "bun run build && wrangler pages deploy dist",
    "format": "prettier --write '**/*.astro' '**/*.ts' '**/*.json'"
  },
  "dependencies": {
    "astro": "^6.1.2",
    "tailwindcss": "^4.2.2",
    "@tailwindcss/vite": "^4.2.2"
  },
  "devDependencies": {
    "prettier": "^3.8.1",
    "prettier-plugin-astro": "^0.14.1",
    "prettier-plugin-tailwindcss": "^0.6.11",
    "wrangler": "^4.79.0"
  }
}
